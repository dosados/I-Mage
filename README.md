# I-Mage

**Stage 1 — work in progress.** Семантический поиск по фотографиям на своём компьютере.

## Задумка

Локальное приложение для навигации по личной медиатеке: индексировать фото, строить эмбеддинги (CLIP) и искать снимки по текстовому описанию — «кот на диване», «закат на море» и т.п.

Долгосрочно — персистентный индекс, поиск по примеру изображения, другие ML-модули (лица, сцены, видео). Полное видение: [docs/goal.md](docs/goal.md).

## Сейчас

Текстовый запрос → CLIP-эмбеддинг → сравнение с фото в каталоге → top-k лучших совпадений. Поиск **на лету** (без сохранённого индекса); для больших библиотек нужен `limit`.


## Быстрый старт

1. Поднять Qdrant-сервер (docker):

```bash
docker compose up -d          # или: sudo docker compose up -d
```

2. Запустить приложение:

```bash
conda activate ml-env
./run.sh                      # автоматически использует QDRANT_URL=http://127.0.0.1:6333
```

Открыть http://127.0.0.1:8000

> **Qdrant работает только как сервер (docker).** `run.sh` ходит на
> `http://127.0.0.1:6333` (переопределяется через `QDRANT_URL`). Встроенный
> файловый режим удалён: его single-writer лок ломал запуск под несколькими
> процессами / `--reload` и рассинхронизировал индекс.
>
> **Не запускайте `uvicorn --reload` во время индексации** — перезапуск обрывает прогон.
> Для разработки с авто-перезагрузкой есть `./run-dev.sh` (не следит за `data/`).

Опционально: каталог с фото через `IMAGE_SEARCH_DIR`, датасет для тестов — `python scripts/download_data.py`. Для поиска по лицу: `python scripts/download_celeba.py`, затем быстрый сабсет `python scripts/build_small_celeba.py` → `data/small_celeba/` (по умолчанию для face search), полный CelebA — через `FACE_SEARCH_DIR`.

## Чеклист

### Сделано

- [x] CLIP (`openai/clip-vit-base-patch32`): `encode_text` / `encode_image`
- [x] Интерфейс `EmbeddingModel`, слой `ml/embeddings`
- [x] Поиск по описанию, top-k, косинусное сходство
- [x] Рекурсивный обход каталога (`io_utils`)
- [x] FastAPI: `GET /health`, `POST /search`, `GET /images/{filename}`, `GET /`
- [x] Веб-UI: промпт, лимит фото, top-k, превью результатов
- [x] CLI-обёртка над API (`clients/cli`)


### Не сделано

- [ ] Персистентный индекс (FAISS, Chroma, Qdrant, …)
- [ ] Фоновая индексация и watch на файловую систему
- [ ] Поиск по примеру изображения
- [ ] Видео, лица, детекция объектов, описание сцен
- [ ] Docker / упаковка в desktop-приложение
- [ ] `requirements.txt`, LICENSE, автотесты
- [ ] Авторизация, кеширование, мультипользовательский режим

Подробнее про scope MVP: [docs/mvp.md](docs/mvp.md).
