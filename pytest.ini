[pytest]
testpaths = tests
pythonpath = . tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    stress: slower stress / latency benchmarks (deselect with -m "not stress")
    api: HTTP API tests via FastAPI TestClient
